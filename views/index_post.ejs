<!DOCTYPE html>
<html lang="en">
<head>
	<style>
	header {
		background: url('<%= backpacker.background %>');
		background-size: cover;
		background-position: center;
		text-align: center;
		opacity: 0.8;
	}
	h1 {
		padding-top: 20px;
	}
	img.round {
	border: 1px solid white;
	border-radius: 70px;
	}
	body {
		text-align: center;
	}
	img {
		position: center;
	}
	li {
		display: block;
	}
	</style>
</head>
<body>
<header>
	<h1><%= backpacker.name %></h1>
	<img src="<%= backpacker.image %>" width="120" height="120" class="round"/>
</header>
	
	<ul>
	<% for (var i = 0; i < posts.length; i++) { %>
		<li>
		<a href="/backpacker/<%= backpacker.id %>/post/<%= posts[i].p_id %>"> <%= posts[i].p_title %></a><br>
		<img src="<%= posts[i].p_image %>" width="auto" height="120px"/><br>
		</li><br>
	<% } %>
	</ul>
	<br>
	<br>
	<a href="/backpacker/<%= backpacker.id %>/posts/new"><%= backpacker.name %>? Make a new post!</a>
	<br>
	<br>
	<a href="/backpackers">Back to list of globetrotters</a>
	<br>
	<br>
</body>
</html>